using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InstantiateAnimations : MonoBehaviour
{
    // Start is called before the first frame update

    // If i find the time or anyone to get some prefabs to spawn when the player completes a tast like getting a hole in a certain number of strokes

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
